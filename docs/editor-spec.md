# 게시글 작성 에디터 기획서

## 개요

관리자 대시보드에서 구조화된 게시글을 쉽게 작성할 수 있는 에디터

---

## 에디터 UI 구성

```
┌─────────────────────────────────────────────────────────────────┐
│  📝 게시글 작성                                    [저장] [취소] │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  제목: [______________________________]                         │
│                                                                 │
│  요약: [______________________________]                         │
│                                                                 │
│  카테고리: [공지 ▼]   썸네일: [파일 선택]                        │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ [■섹션] [1.숫자] [•불릿] [→들여쓰기] │ [작성] [미리보기] │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │                                                         │   │
│  │  (텍스트 입력 영역)                                      │   │
│  │                                                         │   │
│  │                                                         │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 툴바 버튼 기능

### 1. ■ 섹션 제목 버튼
- **클릭 시**: 커서 위치에 `■ ` 삽입
- **키보드 단축키**: `Ctrl + H`
- **결과 예시**:
  ```
  ■ 중소기업 정책자금
  ```
- **렌더링**: 파란색 라인 + 굵은 제목

### 2. 숫자 리스트 버튼
- **클릭 시**: 새 줄에 `1. ` 삽입 (이전 숫자 자동 증가)
- **키보드 단축키**: `Ctrl + 1`
- **결과 예시**:
  ```
  1. 서류 준비
  2. 온라인 신청
  3. 심사 대기
  ```
- **렌더링**: 동그라미 숫자 배지 리스트

### 3. 불릿 리스트 버튼
- **클릭 시**: 새 줄에 `- ` 삽입
- **키보드 단축키**: `Ctrl + -`
- **결과 예시**:
  ```
  - 신청기간: 2025년 1월 ~ 6월
  - 대상: 중소기업
  - 한도: 최대 100억원
  ```
- **렌더링**: 파란색 점 리스트

### 4. 들여쓰기 (하위 항목) 버튼
- **클릭 시**: 새 줄에 `   - ` 삽입 (공백 3개 + 대시)
- **키보드 단축키**: `Tab` (리스트 내에서)
- **결과 예시**:
  ```
  1. 서류 준비
     - 사업계획서
     - 재무제표
     - 기술자료
  ```
- **렌더링**: 회색 들여쓰기 항목

---

## 작성/미리보기 탭

### 작성 탭 (기본)
- 순수 텍스트 입력 (textarea)
- 툴바 버튼으로 서식 삽입
- 문법 하이라이팅 (선택적)

### 미리보기 탭
- 실시간 렌더링 결과 표시
- post.html과 동일한 스타일 적용
- 읽기 전용

---

## 작성 가이드 (도움말)

에디터 하단 또는 사이드에 표시:

```
📖 작성 가이드

■ 섹션 제목    → 파란색 라인 제목
1. 2. 3.      → 숫자 리스트
- 항목        → 불릿 리스트
   - 하위항목  → 들여쓰기 항목 (공백 3개 + -)

💡 빈 줄로 단락을 구분하세요
```

---

## 기술 구현

### 프론트엔드 (dashboard/posts.html)

```html
<!-- 에디터 컴포넌트 -->
<div class="editor-container">
    <!-- 툴바 -->
    <div class="editor-toolbar">
        <button onclick="insertSection()" title="섹션 제목 (Ctrl+H)">■ 섹션</button>
        <button onclick="insertNumber()" title="숫자 리스트 (Ctrl+1)">1. 숫자</button>
        <button onclick="insertBullet()" title="불릿 리스트 (Ctrl+-)">• 불릿</button>
        <button onclick="insertIndent()" title="들여쓰기 (Tab)">→ 들여쓰기</button>
        <span class="divider"></span>
        <button class="tab active" data-tab="write">작성</button>
        <button class="tab" data-tab="preview">미리보기</button>
    </div>

    <!-- 작성 영역 -->
    <textarea id="content-editor" class="editor-textarea"></textarea>

    <!-- 미리보기 영역 -->
    <div id="content-preview" class="editor-preview" style="display:none;"></div>
</div>
```

### JavaScript 함수

```javascript
// 섹션 제목 삽입
function insertSection() {
    insertAtCursor('\n■ ');
}

// 숫자 리스트 삽입
function insertNumber() {
    const nextNum = getNextNumber();
    insertAtCursor(`\n${nextNum}. `);
}

// 불릿 리스트 삽입
function insertBullet() {
    insertAtCursor('\n- ');
}

// 들여쓰기 삽입
function insertIndent() {
    insertAtCursor('\n   - ');
}

// 커서 위치에 텍스트 삽입
function insertAtCursor(text) {
    const editor = document.getElementById('content-editor');
    const start = editor.selectionStart;
    const end = editor.selectionEnd;
    const value = editor.value;

    editor.value = value.substring(0, start) + text + value.substring(end);
    editor.selectionStart = editor.selectionEnd = start + text.length;
    editor.focus();
}

// 미리보기 렌더링 (post.html의 formatStructuredContent 재사용)
function renderPreview() {
    const content = document.getElementById('content-editor').value;
    const preview = document.getElementById('content-preview');
    preview.innerHTML = formatStructuredContent(content);
}
```

---

## API 연동

### 저장 (Airtable)

```javascript
async function savePost() {
    const data = {
        제목: document.getElementById('title').value,
        요약: document.getElementById('summary').value,
        내용: document.getElementById('content-editor').value,
        카테고리: document.getElementById('category').value,
        작성일: new Date().toISOString().split('T')[0],
        공개여부: true
    };

    const response = await fetch('/api/board', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data)
    });

    if (response.ok) {
        alert('저장되었습니다');
        window.location.href = '/dashboard/posts.html';
    }
}
```

---

## 파일 구조

```
dashboard/
├── index.html          # 대시보드 메인
├── posts.html          # 게시글 목록/관리
├── post-edit.html      # 게시글 작성/수정 에디터
└── images.html         # 이미지 관리
```

---

## 구현 우선순위

### Phase 1: 기본 에디터
- [ ] 기본 textarea 에디터
- [ ] 툴바 버튼 4개 (섹션, 숫자, 불릿, 들여쓰기)
- [ ] 삽입 기능

### Phase 2: 미리보기
- [ ] 작성/미리보기 탭 전환
- [ ] formatStructuredContent 재사용
- [ ] 실시간 미리보기

### Phase 3: 저장 기능
- [ ] Airtable POST API
- [ ] 게시글 수정 기능
- [ ] 썸네일 업로드

### Phase 4: UX 개선
- [ ] 키보드 단축키
- [ ] 자동 숫자 증가
- [ ] 문법 하이라이팅 (선택적)

---

## 참고

- 기존 formatStructuredContent 함수: `post.html:1321-1388`
- Airtable 게시판 테이블 ID: `tblvARTwWZRjnft2B`
- API 토큰: `patDHBvdU13xCyo0j...`
